[pytest]
testpaths = tests
markers =
    unit: fast, isolated tests of a single function or class
    integration: tests crossing module boundaries with real dependencies
    e2e: full pipeline end-to-end tests
